{
  "name": "job-hunter-backend",
  "version": "1.0.0",
  "description": "Backend API for Job Hunter with Native SQLite and AI Agent",
  "main": "server.js",
  "scripts": {
    "start": "node --experimental-sqlite server.js",
    "dev": "node --watch --experimental-sqlite server.js",
    "migrate": "node --experimental-sqlite scripts/migrate.js",
    "seed": "node --experimental-sqlite scripts/seed.js",
    "db:reset": "node --experimental-sqlite scripts/reset-db.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "@langchain/core": "^0.3.0",
    "@langchain/langgraph": "^0.2.0",
    "@langchain/openai": "^0.3.0",
    "@langchain/anthropic": "^0.3.0",
    "@langchain/ollama": "^0.1.0",
    "zod": "^3.23.0"
  }
}
